# 为什么 AI Gateway 需要 Provider 中立

很多系统一开始会把业务代码直接写到某一个模型 SDK 上。这样做像把整间厨房的水管都焊在一个品牌的水龙头上：水龙头一换，厨房就要重装。

更稳妥的方式是先定义自己的输入、输出、超时、错误和预算合同，再在后面接入 MiniMax、OpenAI 或其他模型。这样换模型时主要替换适配器，内容工作流不需要重写。

AI Video Studio 延续这个思路：规划、分镜、配音和渲染各自有清晰接口，模型不可用时仍然能生成可审阅的确定性结果。
